spring:
  application.name: usersession
  datasource:
    url: jdbc:h2:file:/db/h2/demo
    username: sa
    password: 
    driverClassName: org.h2.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true


